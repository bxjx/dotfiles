[user]
	name = B.J. Rossiter
	email = b.j.rossiter@gmail.com
[color]
	diff = auto
	status = auto
	branch = auto
[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green
[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red bold
  new = green bold
[color "status"]
  added = yellow
  changed = green
  untracked = cyan

[color]
  ui = true
  [core]
  whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol

[alias]
  st = status
  ci = commit
  ca = commit -a
  br = branch
  co = checkout
  pl = pull --rebase
  pu = push
  df = diff
  lg = log -p
  unpushed = log ORIG_HEAD.. --stat --no-merges
  llog = log --date=local
#  lg = log -graph -pretty=format:\”%Cred%h%Creset - %s %Cgreen(%cr)%Creset\” -abbrev-commit -date=relative
